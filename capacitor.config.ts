import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.workorder.app',
  appName: 'Work Order App',
  webDir: 'dist/public'
};

export default config;
